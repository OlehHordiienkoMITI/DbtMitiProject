
name: 'dbt_miti_project'
version: '1.0.0'
config-version: 2

profile: 'default'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:         
  - "target"
  - "dbt_packages"

models:
  dbt_miti_project:
    racial_testing_deaths:
      materialized: view
    stating_racial_deaths:
      materialized: table
    states:
      materialized: table
    term-care:
      materialized: view